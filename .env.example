# Public environment variables (exposed to client)
# Copy this file to `.env.local` and fill in your values.

# Shared Ko‑fi URL used on the Support Me page (all locales)
PUBLIC_KOFI_URL=https://ko-fi.com/your-profile